P3.1.2 (2026-09-22): mainwindow Filter menu 5 submenus + 16 actions

Reorganize the P0-5 (2026-09-10) flat "滤镜" menu (4 action) into 5
PS-style submenu matching the FilterStrategy category breakdown:
  - 模糊 (4): GaussianBlur / BoxBlur / MedianBlur / BilateralBlur
  - 锐化 (3): Sharpen / SharpenMore / UnsharpMask
  - 风格化 (3): Emboss / FindEdges / GlowingEdges
  - 颜色 (5): Desaturate / Invert / Threshold / Posterize / GradientMap
  - 其他 (5): PhotoFilter / BlurMore / HighPass / Solarize / FilterGallery
  - Total: 20 滤镜 PS 同款 (P0-5 已有 4, P3.1.2 新接 16)

Root cause: P0-5 only wired 4 of 20 FilterKinds to the mainwindow menu.
The other 16 (BoxBlur/MedianBlur/BilateralBlur/SharpenMore/UnsharpMask/
FindEdges/GlowingEdges/Desaturate/Invert/Threshold/Posterize/GradientMap/
BlurMore/HighPass/Solarize/FilterGallery) had the wrong clickable surface —
Factory/Command/Dialog were ready, but the menu entry was missing.

Implementation:
  - Helper lambda `addFilterAction(menu, text, kind)` adds an action with
    a connect to onFilterMenuTriggered(kind) — eliminates 16 copies of the
    same boilerplate.
  - Curves / Levels / B&W / ChannelMixer (P0 leftover 5) + Liquifier (P1.2.5+6+8)
    remain flat at the bottom of the 滤镜 menu (separator-separated from
    the 5 submenu).
  - Status bar "已应用 %1" path (mainwindow.cpp onFilterMenuTriggered) is
    unchanged; P3.1.3 will replace the lambda body with preview-driven flow.

Files:
  - src/app/mainwindow.cpp:  mFilter 改成 5 submenu, 16 action +
                                   addFilterAction helper
  - translatefile/vistella_*.ts:  lupdate picked up 16 new "X..." labels
                                     + 5 new submenu titles

Tests: 60/60 ctest pass (no regression).
  - tst_F_J_MainMenuStructure::menuActions_5to8Each still passes (only
    validates the name list, not live menu state).

Verification:
  - build_local.cmd exit 0
  - ctest -j 4: 60/60 passed in 5.37s